# Generated by training/train.py
# (git ignore has no trailing-comment syntax - keep patterns on their own line)

# cached supervised tables (rebuilt from the xlsx)
data/*.pkl
# trained models (rf_model.pkl is ~80 MB) and their exported JSON - the
# canonical copy of an export lives at client/src/data/riskModel.json and
# server/src/data/riskModel.json (tracked there); regenerate this one with
# training/export_model.py if you need it locally.
models/*.pkl
models/*.json

venv/
__pycache__/
.pytest_cache/

# Keep: results/*.json  (small evaluation reports are worth versioning)

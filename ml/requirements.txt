# Lower bounds, not exact pins - this code doesn't need bleeding-edge versions
# of any of these, and exact-pinning to one dev machine's versions breaks
# installs in environments that ship their own compatible versions already
# (e.g. Google Colab, which needs pandas==2.2.3 and numpy<2.3 for its own
# preinstalled packages - pinning past those forces a conflicting upgrade).
pandas>=2.0
numpy>=1.24
scikit-learn>=1.2
scipy>=1.10
joblib>=1.2
openpyxl>=3.1

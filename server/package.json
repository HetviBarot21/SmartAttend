{
  "name": "smartattend-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tier 2 simulated backend - Express + SQLite, mock RFID and fingerprint capture",
  "main": "src/server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env src/server.js",
    "dev": "node --env-file-if-exists=.env --watch src/server.js",
    "db:init": "node src/db/init.js",
    "db:seed": "node src/db/seed.js",
    "db:seed-history": "node src/db/seedHistory.js",
    "db:reset": "node src/db/init.js --reset && node src/db/seed.js && node src/db/seedHistory.js",
    "test": "node --test src/**/*.test.js"
  },
  "dependencies": {
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "better-sqlite3": "^13.0.3",
    "express": "^4.21.2",
    "morgan": "^1.10.0"
  }
}
